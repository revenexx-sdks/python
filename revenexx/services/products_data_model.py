from ..service import Service
from typing import Any, Dict, List, Optional, Union
from ..exception import RevenexxException
from ..utils.deprecated import deprecated
from ..models.error import Error;
from ..enums.entity_type import EntityType;
from ..enums.kind import Kind;

class ProductsDataModel(Service):

    def __init__(self, client) -> None:
        super(ProductsDataModel, self).__init__(client)

    def products_asset_families_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        labels: Optional[str] = None,
        naming_convention: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        A class of media with one shared shape — packshots, datasheets, line drawings. The family decides which attributes an asset of it carries (alt text, copyright, an expiry date) and, through `naming_convention`, how a file of it is named — which is what lets an import bind a file to a product with no mapping table.
        
        Every column of `asset_families` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The asset family's stable identifier — a class of media with one shared shape. Unique per tenant.
        labels : Optional[str]
            Exact match on `labels`. What the asset family is called, per language tag. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        naming_convention : Optional[str]
            Exact match on `naming_convention`. How a file of this family is named, so an import can bind a file to a product without a mapping table. `source` is the product value the file name is built from, `pattern` how it is assembled, `allowed_extensions` what may be uploaded. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/asset_families'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if naming_convention is not None:
            api_params['naming_convention'] = self._normalize_value(naming_convention)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_asset_families_create(
        self,
        code: str,
        labels: Optional[Dict[str, Any]] = None,
        naming_convention: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one asset family and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        A class of media with one shared shape — packshots, datasheets, line drawings. The family decides which attributes an asset of it carries (alt text, copyright, an expiry date) and, through `naming_convention`, how a file of it is named — which is what lets an import bind a file to a product with no mapping table.
        
        `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The asset family's stable identifier — a class of media with one shared shape. Unique per tenant.
        labels : Optional[Dict[str, Any]]
            What the asset family is called, per language tag.
        naming_convention : Optional[Dict[str, Any]]
            How a file of this family is named, so an import can bind a file to a product without a mapping table. `source` is the product value the file name is built from, `pattern` how it is assembled, `allowed_extensions` what may be uploaded.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/asset_families'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')


        api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        api_params['naming_convention'] = self._normalize_value(naming_convention)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_asset_families_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one asset family by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        It takes what hangs off it: assets (`asset_family_id`) are deleted with it.
        
        An id no asset family of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `asset_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/asset_families`. An id no asset familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/asset_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_asset_families_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one asset family by its id — the whole row, every column, as it is stored.
        
        A class of media with one shared shape — packshots, datasheets, line drawings. The family decides which attributes an asset of it carries (alt text, copyright, an expiry date) and, through `naming_convention`, how a file of it is named — which is what lets an import bind a file to a product with no mapping table.
        
        An id no asset family of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `asset_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/asset_families`. An id no asset familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/asset_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_asset_families_update(
        self,
        id: str,
        code: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None,
        naming_convention: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one asset family by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        A class of media with one shared shape — packshots, datasheets, line drawings. The family decides which attributes an asset of it carries (alt text, copyright, an expiry date) and, through `naming_convention`, how a file of it is named — which is what lets an import bind a file to a product with no mapping table.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `asset_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/asset_families`. An id no asset familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The asset family's stable identifier — a class of media with one shared shape. Unique per tenant.
        labels : Optional[Dict[str, Any]]
            What the asset family is called, per language tag.
        naming_convention : Optional[Dict[str, Any]]
            How a file of this family is named, so an import can bind a file to a product without a mapping table. `source` is the product value the file name is built from, `pattern` how it is assembled, `allowed_extensions` what may be uploaded.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/asset_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        api_params['naming_convention'] = self._normalize_value(naming_convention)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_association_types_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        is_two_way: Optional[bool] = None,
        is_quantified: Optional[bool] = None,
        labels: Optional[str] = None,
        created_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        The KIND of relation two products can have — cross-sell, accessory, spare part, bill of materials. `is_two_way` declares the relation symmetric and `is_quantified` declares that it carries a quantity; both are declarations a client READS rather than behaviour this app performs — it stores one row per direction and never creates the mirror for you.
        
        Every column of `association_types` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The kind of relation between two products. Unique per tenant.
        is_two_way : Optional[bool]
            Exact match on `is_two_way`. Declares the relation symmetric — an accessory of A is an accessory of B. It is a declaration a client reads: this app stores one row per direction and does not create the mirror for you.
        is_quantified : Optional[bool]
            Exact match on `is_quantified`. Declares that a relation of this kind carries a quantity — a bundle, a bill of materials. `product_associations.quantity` is where that number goes, and it is meaningless without this flag.
        labels : Optional[str]
            Exact match on `labels`. What the relation is called in a product form, per language tag. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/association_types'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if is_two_way is not None:
            api_params['is_two_way'] = self._normalize_value(is_two_way)
        if is_quantified is not None:
            api_params['is_quantified'] = self._normalize_value(is_quantified)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_association_types_create(
        self,
        code: str,
        is_quantified: Optional[bool] = None,
        is_two_way: Optional[bool] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one association type and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        The KIND of relation two products can have — cross-sell, accessory, spare part, bill of materials. `is_two_way` declares the relation symmetric and `is_quantified` declares that it carries a quantity; both are declarations a client READS rather than behaviour this app performs — it stores one row per direction and never creates the mirror for you.
        
        `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The kind of relation between two products. Unique per tenant.
        is_quantified : Optional[bool]
            Declares that a relation of this kind carries a quantity — a bundle, a bill of materials. `product_associations.quantity` is where that number goes, and it is meaningless without this flag.
        is_two_way : Optional[bool]
            Declares the relation symmetric — an accessory of A is an accessory of B. It is a declaration a client reads: this app stores one row per direction and does not create the mirror for you.
        labels : Optional[Dict[str, Any]]
            What the relation is called in a product form, per language tag.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/association_types'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')


        api_params['code'] = self._normalize_value(code)
        if is_quantified is not None:
            api_params['is_quantified'] = self._normalize_value(is_quantified)
        if is_two_way is not None:
            api_params['is_two_way'] = self._normalize_value(is_two_way)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_association_types_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one association type by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        It takes what hangs off it: product associations (`association_type_id`) are deleted with it.
        
        An id no association type of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `association_types` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/association_types`. An id no association type of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/association_types/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_association_types_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one association type by its id — the whole row, every column, as it is stored.
        
        The KIND of relation two products can have — cross-sell, accessory, spare part, bill of materials. `is_two_way` declares the relation symmetric and `is_quantified` declares that it carries a quantity; both are declarations a client READS rather than behaviour this app performs — it stores one row per direction and never creates the mirror for you.
        
        An id no association type of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `association_types` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/association_types`. An id no association type of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/association_types/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_association_types_update(
        self,
        id: str,
        code: Optional[str] = None,
        is_quantified: Optional[bool] = None,
        is_two_way: Optional[bool] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one association type by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        The KIND of relation two products can have — cross-sell, accessory, spare part, bill of materials. `is_two_way` declares the relation symmetric and `is_quantified` declares that it carries a quantity; both are declarations a client READS rather than behaviour this app performs — it stores one row per direction and never creates the mirror for you.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `association_types` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/association_types`. An id no association type of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The kind of relation between two products. Unique per tenant.
        is_quantified : Optional[bool]
            Declares that a relation of this kind carries a quantity — a bundle, a bill of materials. `product_associations.quantity` is where that number goes, and it is meaningless without this flag.
        is_two_way : Optional[bool]
            Declares the relation symmetric — an accessory of A is an accessory of B. It is a declaration a client reads: this app stores one row per direction and does not create the mirror for you.
        labels : Optional[Dict[str, Any]]
            What the relation is called in a product form, per language tag.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/association_types/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if is_quantified is not None:
            api_params['is_quantified'] = self._normalize_value(is_quantified)
        if is_two_way is not None:
            api_params['is_two_way'] = self._normalize_value(is_two_way)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_schema(
        self,
        family_id: Optional[str] = None,
        family_code: Optional[str] = None,
        entity_type: Optional[EntityType] = None,
        entity_ref: Optional[str] = None,
        locale: Optional[str] = None,
        channel: Optional[str] = None,
        kind: Optional[Kind] = None
    ) -> Error:
        """
        Which fields does this family have — one ready-to-render list, not six joined tables. The catalog's SHAPE is tenant data: a product's properties are rows in `attributes`, grouped by `attribute_groups`, selected per family by `family_attributes`, with their permitted values in `attribute_options` and their variant axes in `family_variants`. Reading that shape used to mean five reads, a join, and a private `attributes.type` → input mapping in every client — and that mapping is the part that must live here, because the type list carries no CHECK by design and an integrator extends it. Answers one field list instead, ordered by group then by the family's own ordering. Without a family it answers every attribute declared for `entity_type`/`entity_ref` — the shape of a reference entity's records or an asset family, which have attributes but no family. Writes nothing.

        Parameters
        ----------
        family_id : Optional[str]
            The family whose fields to describe. A family that does not exist answers 404 — it is not silently ignored.
        family_code : Optional[str]
            Alternative to family_id — a `families.code` THIS TENANT holds, from `GET /products/families`. Wins nothing over family_id: when both are sent, family_id is resolved and family_code ignored. A code no family carries answers 404, so this parameter carries no example — every value is somebody's 404.
        entity_type : Optional[EntityType]
            Attribute entity when no family is named. Default 'product'. The four this app's own model is built on; the column carries no CHECK, so a tenant that has invented a fifth is answered on it too.
        entity_ref : Optional[str]
            Narrows entity_type to one reference entity / asset family, by its code.
        locale : Optional[str]
            Resolves labels and the storage path of every localizable field. Omitted: labels fall back to English and localizable fields report a null path.
        channel : Optional[str]
            Resolves the storage path of every scopable field, and narrows `required` to the channels the family requires the attribute in.
        kind : Optional[Kind]
            The product kind the fields are for. 'model' locks the family's variant axes, which are set on the variants.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute-schema'
        api_params = {}

        if family_id is not None:
            api_params['family_id'] = self._normalize_value(family_id)
        if family_code is not None:
            api_params['family_code'] = self._normalize_value(family_code)
        if entity_type is not None:
            api_params['entity_type'] = self._normalize_value(entity_type)
        if entity_ref is not None:
            api_params['entity_ref'] = self._normalize_value(entity_ref)
        if locale is not None:
            api_params['locale'] = self._normalize_value(locale)
        if channel is not None:
            api_params['channel'] = self._normalize_value(channel)
        if kind is not None:
            api_params['kind'] = self._normalize_value(kind)

        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_groups_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        position: Optional[float] = None,
        labels: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        An attribute group is a SECTION of a product form — "Technical attributes", "Logistics" — and the thing every attribute is filed under. It carries a `position`, which is the order the sections appear in, and per-language `labels`, which is what an operator reads; the `code` is what an attribute joins on and is never shown. `GET /products/attribute-schema` already resolves a group's heading onto every field it returns, so these routes are for MANAGING the sections, not for rendering a form.
        
        Every column of `attribute_groups` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The group's stable identifier, and the value an `AttributeField` carries as its `group` — a SECTION of the product form, not a label. Unique per tenant and the key an import joins on.
        position : Optional[float]
            Exact match on `position`. Where this section sits in a form, ascending. Sections that tie keep the order the database returns them in.
        labels : Optional[str]
            Exact match on `labels`. The section heading a person sees, keyed by language tag. The code is never shown to an operator; a tag nobody translated falls back to the next filled one, then to English. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_groups'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_attribute_groups_create(
        self,
        code: str,
        labels: Optional[Dict[str, Any]] = None,
        position: Optional[float] = None
    ) -> Error:
        """
        Creates one attribute group and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        An attribute group is a SECTION of a product form — "Technical attributes", "Logistics" — and the thing every attribute is filed under. It carries a `position`, which is the order the sections appear in, and per-language `labels`, which is what an operator reads; the `code` is what an attribute joins on and is never shown. `GET /products/attribute-schema` already resolves a group's heading onto every field it returns, so these routes are for MANAGING the sections, not for rendering a form.
        
        `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The group's stable identifier, and the value an `AttributeField` carries as its `group` — a SECTION of the product form, not a label. Unique per tenant and the key an import joins on.
        labels : Optional[Dict[str, Any]]
            The section heading a person sees, keyed by language tag. The code is never shown to an operator; a tag nobody translated falls back to the next filled one, then to English.
        position : Optional[float]
            Where this section sits in a form, ascending. Sections that tie keep the order the database returns them in.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_groups'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')


        api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        if position is not None:
            api_params['position'] = self._normalize_value(position)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_groups_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one attribute group by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        `attributes.group_id` is set to null instead, so the rows that pointed at it survive the delete rather than going with it.
        
        An id no attribute group of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `attribute_groups` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_groups`. An id no attribute group of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_groups/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_groups_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one attribute group by its id — the whole row, every column, as it is stored.
        
        An attribute group is a SECTION of a product form — "Technical attributes", "Logistics" — and the thing every attribute is filed under. It carries a `position`, which is the order the sections appear in, and per-language `labels`, which is what an operator reads; the `code` is what an attribute joins on and is never shown. `GET /products/attribute-schema` already resolves a group's heading onto every field it returns, so these routes are for MANAGING the sections, not for rendering a form.
        
        An id no attribute group of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `attribute_groups` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_groups`. An id no attribute group of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_groups/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_groups_update(
        self,
        id: str,
        code: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None,
        position: Optional[float] = None
    ) -> Error:
        """
        Updates one attribute group by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        An attribute group is a SECTION of a product form — "Technical attributes", "Logistics" — and the thing every attribute is filed under. It carries a `position`, which is the order the sections appear in, and per-language `labels`, which is what an operator reads; the `code` is what an attribute joins on and is never shown. `GET /products/attribute-schema` already resolves a group's heading onto every field it returns, so these routes are for MANAGING the sections, not for rendering a form.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `attribute_groups` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_groups`. An id no attribute group of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The group's stable identifier, and the value an `AttributeField` carries as its `group` — a SECTION of the product form, not a label. Unique per tenant and the key an import joins on.
        labels : Optional[Dict[str, Any]]
            The section heading a person sees, keyed by language tag. The code is never shown to an operator; a tag nobody translated falls back to the next filled one, then to English.
        position : Optional[float]
            Where this section sits in a form, ascending. Sections that tie keep the order the database returns them in.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_groups/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        if position is not None:
            api_params['position'] = self._normalize_value(position)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_options_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        attribute_id: Optional[str] = None,
        code: Optional[str] = None,
        position: Optional[float] = None,
        swatch: Optional[str] = None,
        labels: Optional[str] = None,
        created_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        The permitted values of one select or multi-select attribute. A record stores the option's CODE and never its label, so renaming an option in every language leaves every product that picked it untouched, and `position` is the order it appears in the dropdown. `GET /products/attribute-schema` republishes these as a field's `options`, already resolved for a locale.
        
        Every column of `attribute_options` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        attribute_id : Optional[str]
            Exact match on `attribute_id`. The select / multi-select attribute these are the permitted values of. Deleting the attribute deletes its options with it.
        code : Optional[str]
            Exact match on `code`. The value actually STORED in a record's `attribute_values` when this option is picked — never the label. Unique within the attribute.
        position : Optional[float]
            Exact match on `position`. Order in the dropdown, ascending. Options that tie keep the order the database returns them in, so give every option a position if the order matters.
        swatch : Optional[str]
            Exact match on `swatch`. A colour or texture chip for the picker. Null for an option that is not visual. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        labels : Optional[str]
            Exact match on `labels`. What the option is called, per language tag. Two tenants may label the same code differently; only the code is ever written into a record. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_options'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if attribute_id is not None:
            api_params['attribute_id'] = self._normalize_value(attribute_id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if swatch is not None:
            api_params['swatch'] = self._normalize_value(swatch)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_attribute_options_create(
        self,
        attribute_id: str,
        code: str,
        labels: Optional[Dict[str, Any]] = None,
        position: Optional[float] = None,
        swatch: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one attribute option and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        The permitted values of one select or multi-select attribute. A record stores the option's CODE and never its label, so renaming an option in every language leaves every product that picked it untouched, and `position` is the order it appears in the dropdown. `GET /products/attribute-schema` republishes these as a field's `options`, already resolved for a locale.
        
        `attribute_id` and `code` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `attribute_id` and `code` answers 409.

        Parameters
        ----------
        attribute_id : str
            The select / multi-select attribute these are the permitted values of. Deleting the attribute deletes its options with it.
        code : str
            The value actually STORED in a record's `attribute_values` when this option is picked — never the label. Unique within the attribute.
        labels : Optional[Dict[str, Any]]
            What the option is called, per language tag. Two tenants may label the same code differently; only the code is ever written into a record.
        position : Optional[float]
            Order in the dropdown, ascending. Options that tie keep the order the database returns them in, so give every option a position if the order matters.
        swatch : Optional[Dict[str, Any]]
            A colour or texture chip for the picker. Null for an option that is not visual.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_options'
        api_params = {}
        if attribute_id is None:
            raise RevenexxException('Missing required parameter: "attribute_id"')

        if code is None:
            raise RevenexxException('Missing required parameter: "code"')


        api_params['attribute_id'] = self._normalize_value(attribute_id)
        api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        api_params['swatch'] = self._normalize_value(swatch)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_options_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one attribute option by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        Nothing in this schema references it, so nothing else changes.
        
        An id no attribute option of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `attribute_options` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_options`. An id no attribute option of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_options/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_options_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one attribute option by its id — the whole row, every column, as it is stored.
        
        The permitted values of one select or multi-select attribute. A record stores the option's CODE and never its label, so renaming an option in every language leaves every product that picked it untouched, and `position` is the order it appears in the dropdown. `GET /products/attribute-schema` republishes these as a field's `options`, already resolved for a locale.
        
        An id no attribute option of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `attribute_options` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_options`. An id no attribute option of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_options/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attribute_options_update(
        self,
        id: str,
        attribute_id: Optional[str] = None,
        code: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None,
        position: Optional[float] = None,
        swatch: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one attribute option by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        The permitted values of one select or multi-select attribute. A record stores the option's CODE and never its label, so renaming an option in every language leaves every product that picked it untouched, and `position` is the order it appears in the dropdown. `GET /products/attribute-schema` republishes these as a field's `options`, already resolved for a locale.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `attribute_id` and `code` answers 409.

        Parameters
        ----------
        id : str
            The `attribute_options` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attribute_options`. An id no attribute option of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        attribute_id : Optional[str]
            The select / multi-select attribute these are the permitted values of. Deleting the attribute deletes its options with it.
        code : Optional[str]
            The value actually STORED in a record's `attribute_values` when this option is picked — never the label. Unique within the attribute.
        labels : Optional[Dict[str, Any]]
            What the option is called, per language tag. Two tenants may label the same code differently; only the code is ever written into a record.
        position : Optional[float]
            Order in the dropdown, ascending. Options that tie keep the order the database returns them in, so give every option a position if the order matters.
        swatch : Optional[Dict[str, Any]]
            A colour or texture chip for the picker. Null for an option that is not visual.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attribute_options/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if attribute_id is not None:
            api_params['attribute_id'] = self._normalize_value(attribute_id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        api_params['swatch'] = self._normalize_value(swatch)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attributes_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        entity_type: Optional[str] = None,
        entity_ref: Optional[str] = None,
        type: Optional[str] = None,
        group_id: Optional[str] = None,
        localizable: Optional[bool] = None,
        scopable: Optional[bool] = None,
        is_unique: Optional[bool] = None,
        is_filterable: Optional[bool] = None,
        usable_in_grid: Optional[bool] = None,
        validation: Optional[str] = None,
        config: Optional[str] = None,
        labels: Optional[str] = None,
        position: Optional[float] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        An attribute is one property a record can carry, and in an attribute-driven PIM it is a ROW rather than a column: giving the catalog a "net weight" is a create here, not a migration. Its own flags decide everything downstream — `localizable` and `scopable` pick which of the four `attribute_values` buckets its values are written to, `type` picks the editor that renders it, `usable_in_grid` and `is_filterable` are what the product grid reads. `entity_type`/`entity_ref` say which kind of record carries it: a product, one reference entity's records, one asset family, or a category.
        
        Every column of `attributes` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The attribute's stable identifier — the KEY its value is stored under inside `attribute_values`, and the name a category rule addresses as `attribute:<code>`. Unique per (`entity_type`, `entity_ref`) in this tenant.
        entity_type : Optional[str]
            Exact match on `entity_type`. Which kind of record carries this attribute: 'product' for the catalog itself, 'reference_entity', 'asset' or 'category' for the other things in this app that have attributes. Deliberately carries no CHECK — a tenant that models a fifth kind is served on it too.
        entity_ref : Optional[str]
            Exact match on `entity_ref`. Narrows `entity_type` to ONE reference entity or asset family, by its code — the attributes of `brand` rather than of every reference entity. Null for a plain product attribute.
        type : Optional[str]
            Exact match on `type`. Which editor the value asks for — 'text', 'select', 'metric', 'price', 'asset_collection', 'reference_entity'. Carries no CHECK on purpose: an integrator adds a type, and `GET /products/attribute-schema` maps an unknown one onto a text field rather than refusing to answer.
        group_id : Optional[str]
            Exact match on `group_id`. The `attribute_groups` row this attribute is filed under — the form section it appears in. Null is ungrouped, and an ungrouped field is rendered after every section that has a name.
        localizable : Optional[bool]
            Exact match on `localizable`. True → the record holds ONE VALUE PER LOCALE, under `attribute_values.locale_specific.<locale>.<code>`. False → one value, under `attribute_values.common.<code>`. This flag is what decides where a write goes.
        scopable : Optional[bool]
            Exact match on `scopable`. True → one value PER CHANNEL, under `attribute_values.channel_specific.<channel>.<code>`. Set together with `localizable` it means one value per channel AND locale, in `channel_locale_specific`.
        is_unique : Optional[bool]
            Exact match on `is_unique`. Declares that the value identifies the product — an EAN, a manufacturer part number. It is metadata a form and an importer read: no database index enforces it, because the value lives inside jsonb rather than in a column.
        is_filterable : Optional[bool]
            Exact match on `is_filterable`. Offer this attribute as a filter in a product list. `GET /products/grid` reports exactly these attributes in its `filters` array, and nothing else reads the flag.
        usable_in_grid : Optional[bool]
            Exact match on `usable_in_grid`. Show this attribute as a COLUMN in the product grid. `GET /products/grid` returns a column definition and a per-row value for exactly these.
        validation : Optional[str]
            Exact match on `validation`. Limits a value has to satisfy, as a flat object. The seven keys a client can act on are `min`, `max`, `min_length`, `max_length`, `pattern`, `min_items`, `max_items` — `GET /products/attribute-schema` republishes those and leaves anything else the tenant stored untouched. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        config : Optional[str]
            Exact match on `config`. Type-specific settings; which keys apply depends on `type`. The ones this app reads: `units` (the unit list a measure attribute offers) and `reference_entity` (which entity a reference attribute draws its options from). The ones the cockpit edits alongside them: `unit`, `metric_family`, `decimals_allowed`, `asset_family`, `max_file_size`, `allowed_extensions`. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        labels : Optional[str]
            Exact match on `labels`. The field label a person sees, keyed by language tag. Resolution falls back to English and then to the code, so an untranslated attribute is still renderable. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        position : Optional[float]
            Exact match on `position`. Where the field sits inside its group. A family may override it for its own form through `family_attributes.position`; this is the attribute's default.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attributes'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if entity_type is not None:
            api_params['entity_type'] = self._normalize_value(entity_type)
        if entity_ref is not None:
            api_params['entity_ref'] = self._normalize_value(entity_ref)
        if type is not None:
            api_params['type'] = self._normalize_value(type)
        if group_id is not None:
            api_params['group_id'] = self._normalize_value(group_id)
        if localizable is not None:
            api_params['localizable'] = self._normalize_value(localizable)
        if scopable is not None:
            api_params['scopable'] = self._normalize_value(scopable)
        if is_unique is not None:
            api_params['is_unique'] = self._normalize_value(is_unique)
        if is_filterable is not None:
            api_params['is_filterable'] = self._normalize_value(is_filterable)
        if usable_in_grid is not None:
            api_params['usable_in_grid'] = self._normalize_value(usable_in_grid)
        if validation is not None:
            api_params['validation'] = self._normalize_value(validation)
        if config is not None:
            api_params['config'] = self._normalize_value(config)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_attributes_create(
        self,
        code: str,
        type: str,
        config: Optional[Dict[str, Any]] = None,
        entity_ref: Optional[str] = None,
        entity_type: Optional[str] = None,
        group_id: Optional[str] = None,
        is_filterable: Optional[bool] = None,
        is_unique: Optional[bool] = None,
        labels: Optional[Dict[str, Any]] = None,
        localizable: Optional[bool] = None,
        position: Optional[float] = None,
        scopable: Optional[bool] = None,
        usable_in_grid: Optional[bool] = None,
        validation: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one attribute and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        An attribute is one property a record can carry, and in an attribute-driven PIM it is a ROW rather than a column: giving the catalog a "net weight" is a create here, not a migration. Its own flags decide everything downstream — `localizable` and `scopable` pick which of the four `attribute_values` buckets its values are written to, `type` picks the editor that renders it, `usable_in_grid` and `is_filterable` are what the product grid reads. `entity_type`/`entity_ref` say which kind of record carries it: a product, one reference entity's records, one asset family, or a category.
        
        `code` and `type` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `entity_type`, `entity_ref`, `code` answers 409.

        Parameters
        ----------
        code : str
            The attribute's stable identifier — the KEY its value is stored under inside `attribute_values`, and the name a category rule addresses as `attribute:<code>`. Unique per (`entity_type`, `entity_ref`) in this tenant.
        type : str
            Which editor the value asks for — 'text', 'select', 'metric', 'price', 'asset_collection', 'reference_entity'. Carries no CHECK on purpose: an integrator adds a type, and `GET /products/attribute-schema` maps an unknown one onto a text field rather than refusing to answer.
        config : Optional[Dict[str, Any]]
            Type-specific settings; which keys apply depends on `type`. The ones this app reads: `units` (the unit list a measure attribute offers) and `reference_entity` (which entity a reference attribute draws its options from). The ones the cockpit edits alongside them: `unit`, `metric_family`, `decimals_allowed`, `asset_family`, `max_file_size`, `allowed_extensions`.
        entity_ref : Optional[str]
            Narrows `entity_type` to ONE reference entity or asset family, by its code — the attributes of `brand` rather than of every reference entity. Null for a plain product attribute.
        entity_type : Optional[str]
            Which kind of record carries this attribute: 'product' for the catalog itself, 'reference_entity', 'asset' or 'category' for the other things in this app that have attributes. Deliberately carries no CHECK — a tenant that models a fifth kind is served on it too.
        group_id : Optional[str]
            The `attribute_groups` row this attribute is filed under — the form section it appears in. Null is ungrouped, and an ungrouped field is rendered after every section that has a name.
        is_filterable : Optional[bool]
            Offer this attribute as a filter in a product list. `GET /products/grid` reports exactly these attributes in its `filters` array, and nothing else reads the flag.
        is_unique : Optional[bool]
            Declares that the value identifies the product — an EAN, a manufacturer part number. It is metadata a form and an importer read: no database index enforces it, because the value lives inside jsonb rather than in a column.
        labels : Optional[Dict[str, Any]]
            The field label a person sees, keyed by language tag. Resolution falls back to English and then to the code, so an untranslated attribute is still renderable.
        localizable : Optional[bool]
            True → the record holds ONE VALUE PER LOCALE, under `attribute_values.locale_specific.<locale>.<code>`. False → one value, under `attribute_values.common.<code>`. This flag is what decides where a write goes.
        position : Optional[float]
            Where the field sits inside its group. A family may override it for its own form through `family_attributes.position`; this is the attribute's default.
        scopable : Optional[bool]
            True → one value PER CHANNEL, under `attribute_values.channel_specific.<channel>.<code>`. Set together with `localizable` it means one value per channel AND locale, in `channel_locale_specific`.
        usable_in_grid : Optional[bool]
            Show this attribute as a COLUMN in the product grid. `GET /products/grid` returns a column definition and a per-row value for exactly these.
        validation : Optional[Dict[str, Any]]
            Limits a value has to satisfy, as a flat object. The seven keys a client can act on are `min`, `max`, `min_length`, `max_length`, `pattern`, `min_items`, `max_items` — `GET /products/attribute-schema` republishes those and leaves anything else the tenant stored untouched.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attributes'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')

        if type is None:
            raise RevenexxException('Missing required parameter: "type"')


        api_params['code'] = self._normalize_value(code)
        api_params['config'] = self._normalize_value(config)
        api_params['entity_ref'] = self._normalize_value(entity_ref)
        if entity_type is not None:
            api_params['entity_type'] = self._normalize_value(entity_type)
        api_params['group_id'] = self._normalize_value(group_id)
        if is_filterable is not None:
            api_params['is_filterable'] = self._normalize_value(is_filterable)
        if is_unique is not None:
            api_params['is_unique'] = self._normalize_value(is_unique)
        api_params['labels'] = self._normalize_value(labels)
        if localizable is not None:
            api_params['localizable'] = self._normalize_value(localizable)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if scopable is not None:
            api_params['scopable'] = self._normalize_value(scopable)
        api_params['type'] = self._normalize_value(type)
        if usable_in_grid is not None:
            api_params['usable_in_grid'] = self._normalize_value(usable_in_grid)
        api_params['validation'] = self._normalize_value(validation)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attributes_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one attribute by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        It takes what hangs off it: attribute options (`attribute_id`), family attributes (`attribute_id`) are deleted with it.
        
        An id no attribute of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attributes`. An id no attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attributes_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one attribute by its id — the whole row, every column, as it is stored.
        
        An attribute is one property a record can carry, and in an attribute-driven PIM it is a ROW rather than a column: giving the catalog a "net weight" is a create here, not a migration. Its own flags decide everything downstream — `localizable` and `scopable` pick which of the four `attribute_values` buckets its values are written to, `type` picks the editor that renders it, `usable_in_grid` and `is_filterable` are what the product grid reads. `entity_type`/`entity_ref` say which kind of record carries it: a product, one reference entity's records, one asset family, or a category.
        
        An id no attribute of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attributes`. An id no attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_attributes_update(
        self,
        id: str,
        code: Optional[str] = None,
        config: Optional[Dict[str, Any]] = None,
        entity_ref: Optional[str] = None,
        entity_type: Optional[str] = None,
        group_id: Optional[str] = None,
        is_filterable: Optional[bool] = None,
        is_unique: Optional[bool] = None,
        labels: Optional[Dict[str, Any]] = None,
        localizable: Optional[bool] = None,
        position: Optional[float] = None,
        scopable: Optional[bool] = None,
        type: Optional[str] = None,
        usable_in_grid: Optional[bool] = None,
        validation: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one attribute by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        An attribute is one property a record can carry, and in an attribute-driven PIM it is a ROW rather than a column: giving the catalog a "net weight" is a create here, not a migration. Its own flags decide everything downstream — `localizable` and `scopable` pick which of the four `attribute_values` buckets its values are written to, `type` picks the editor that renders it, `usable_in_grid` and `is_filterable` are what the product grid reads. `entity_type`/`entity_ref` say which kind of record carries it: a product, one reference entity's records, one asset family, or a category.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `entity_type`, `entity_ref`, `code` answers 409.

        Parameters
        ----------
        id : str
            The `attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/attributes`. An id no attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The attribute's stable identifier — the KEY its value is stored under inside `attribute_values`, and the name a category rule addresses as `attribute:<code>`. Unique per (`entity_type`, `entity_ref`) in this tenant.
        config : Optional[Dict[str, Any]]
            Type-specific settings; which keys apply depends on `type`. The ones this app reads: `units` (the unit list a measure attribute offers) and `reference_entity` (which entity a reference attribute draws its options from). The ones the cockpit edits alongside them: `unit`, `metric_family`, `decimals_allowed`, `asset_family`, `max_file_size`, `allowed_extensions`.
        entity_ref : Optional[str]
            Narrows `entity_type` to ONE reference entity or asset family, by its code — the attributes of `brand` rather than of every reference entity. Null for a plain product attribute.
        entity_type : Optional[str]
            Which kind of record carries this attribute: 'product' for the catalog itself, 'reference_entity', 'asset' or 'category' for the other things in this app that have attributes. Deliberately carries no CHECK — a tenant that models a fifth kind is served on it too.
        group_id : Optional[str]
            The `attribute_groups` row this attribute is filed under — the form section it appears in. Null is ungrouped, and an ungrouped field is rendered after every section that has a name.
        is_filterable : Optional[bool]
            Offer this attribute as a filter in a product list. `GET /products/grid` reports exactly these attributes in its `filters` array, and nothing else reads the flag.
        is_unique : Optional[bool]
            Declares that the value identifies the product — an EAN, a manufacturer part number. It is metadata a form and an importer read: no database index enforces it, because the value lives inside jsonb rather than in a column.
        labels : Optional[Dict[str, Any]]
            The field label a person sees, keyed by language tag. Resolution falls back to English and then to the code, so an untranslated attribute is still renderable.
        localizable : Optional[bool]
            True → the record holds ONE VALUE PER LOCALE, under `attribute_values.locale_specific.<locale>.<code>`. False → one value, under `attribute_values.common.<code>`. This flag is what decides where a write goes.
        position : Optional[float]
            Where the field sits inside its group. A family may override it for its own form through `family_attributes.position`; this is the attribute's default.
        scopable : Optional[bool]
            True → one value PER CHANNEL, under `attribute_values.channel_specific.<channel>.<code>`. Set together with `localizable` it means one value per channel AND locale, in `channel_locale_specific`.
        type : Optional[str]
            Which editor the value asks for — 'text', 'select', 'metric', 'price', 'asset_collection', 'reference_entity'. Carries no CHECK on purpose: an integrator adds a type, and `GET /products/attribute-schema` maps an unknown one onto a text field rather than refusing to answer.
        usable_in_grid : Optional[bool]
            Show this attribute as a COLUMN in the product grid. `GET /products/grid` returns a column definition and a per-row value for exactly these.
        validation : Optional[Dict[str, Any]]
            Limits a value has to satisfy, as a flat object. The seven keys a client can act on are `min`, `max`, `min_length`, `max_length`, `pattern`, `min_items`, `max_items` — `GET /products/attribute-schema` republishes those and leaves anything else the tenant stored untouched.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['config'] = self._normalize_value(config)
        api_params['entity_ref'] = self._normalize_value(entity_ref)
        if entity_type is not None:
            api_params['entity_type'] = self._normalize_value(entity_type)
        api_params['group_id'] = self._normalize_value(group_id)
        if is_filterable is not None:
            api_params['is_filterable'] = self._normalize_value(is_filterable)
        if is_unique is not None:
            api_params['is_unique'] = self._normalize_value(is_unique)
        api_params['labels'] = self._normalize_value(labels)
        if localizable is not None:
            api_params['localizable'] = self._normalize_value(localizable)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if scopable is not None:
            api_params['scopable'] = self._normalize_value(scopable)
        if type is not None:
            api_params['type'] = self._normalize_value(type)
        if usable_in_grid is not None:
            api_params['usable_in_grid'] = self._normalize_value(usable_in_grid)
        api_params['validation'] = self._normalize_value(validation)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_families_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        label_attribute: Optional[str] = None,
        image_attribute: Optional[str] = None,
        labels: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        A family decides WHICH attributes a product has — the set is `family_attributes`, and every family-driven surface follows from it. It also names which attribute carries the display name (`label_attribute`) and which carries the main image. A product with no family has no required attributes at all, so its completeness cannot be measured and its name never resolves past the SKU; `POST /products/{id}/family` is the call that ends that state.
        
        Every column of `families` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The family's stable identifier — which set of attributes a product of this family HAS. Unique per tenant, and the value `GET /products/attribute-schema?family_code=` resolves.
        label_attribute : Optional[str]
            Exact match on `label_attribute`. Which attribute CODE carries the display name of a product in this family. A product's name is an attribute, not a column, and which attribute it is, is per family. Null falls back to the `default_label_attribute` setting and then to the conventional `name`.
        image_attribute : Optional[str]
            Exact match on `image_attribute`. Which attribute code carries the product's main image — the one a grid thumbnail and a picker read.
        labels : Optional[str]
            Exact match on `labels`. What the family is called, per language tag — the name an operator picks from, while the code is what everything else joins on. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/families'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if label_attribute is not None:
            api_params['label_attribute'] = self._normalize_value(label_attribute)
        if image_attribute is not None:
            api_params['image_attribute'] = self._normalize_value(image_attribute)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_families_create(
        self,
        code: str,
        image_attribute: Optional[str] = None,
        label_attribute: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one family and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        A family decides WHICH attributes a product has — the set is `family_attributes`, and every family-driven surface follows from it. It also names which attribute carries the display name (`label_attribute`) and which carries the main image. A product with no family has no required attributes at all, so its completeness cannot be measured and its name never resolves past the SKU; `POST /products/{id}/family` is the call that ends that state.
        
        `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The family's stable identifier — which set of attributes a product of this family HAS. Unique per tenant, and the value `GET /products/attribute-schema?family_code=` resolves.
        image_attribute : Optional[str]
            Which attribute code carries the product's main image — the one a grid thumbnail and a picker read.
        label_attribute : Optional[str]
            Which attribute CODE carries the display name of a product in this family. A product's name is an attribute, not a column, and which attribute it is, is per family. Null falls back to the `default_label_attribute` setting and then to the conventional `name`.
        labels : Optional[Dict[str, Any]]
            What the family is called, per language tag — the name an operator picks from, while the code is what everything else joins on.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/families'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')


        api_params['code'] = self._normalize_value(code)
        api_params['image_attribute'] = self._normalize_value(image_attribute)
        api_params['label_attribute'] = self._normalize_value(label_attribute)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_families_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one family by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        It takes what hangs off it: family attributes (`family_id`), family variants (`family_id`) are deleted with it. `products.family_id` is set to null instead, so the rows that pointed at it survive the delete rather than going with it.
        
        An id no family of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/families`. An id no familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_families_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one family by its id — the whole row, every column, as it is stored.
        
        A family decides WHICH attributes a product has — the set is `family_attributes`, and every family-driven surface follows from it. It also names which attribute carries the display name (`label_attribute`) and which carries the main image. A product with no family has no required attributes at all, so its completeness cannot be measured and its name never resolves past the SKU; `POST /products/{id}/family` is the call that ends that state.
        
        An id no family of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/families`. An id no familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_families_update(
        self,
        id: str,
        code: Optional[str] = None,
        image_attribute: Optional[str] = None,
        label_attribute: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one family by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        A family decides WHICH attributes a product has — the set is `family_attributes`, and every family-driven surface follows from it. It also names which attribute carries the display name (`label_attribute`) and which carries the main image. A product with no family has no required attributes at all, so its completeness cannot be measured and its name never resolves past the SKU; `POST /products/{id}/family` is the call that ends that state.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/families`. An id no familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The family's stable identifier — which set of attributes a product of this family HAS. Unique per tenant, and the value `GET /products/attribute-schema?family_code=` resolves.
        image_attribute : Optional[str]
            Which attribute code carries the product's main image — the one a grid thumbnail and a picker read.
        label_attribute : Optional[str]
            Which attribute CODE carries the display name of a product in this family. A product's name is an attribute, not a column, and which attribute it is, is per family. Null falls back to the `default_label_attribute` setting and then to the conventional `name`.
        labels : Optional[Dict[str, Any]]
            What the family is called, per language tag — the name an operator picks from, while the code is what everything else joins on.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['image_attribute'] = self._normalize_value(image_attribute)
        api_params['label_attribute'] = self._normalize_value(label_attribute)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_attributes_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        family_id: Optional[str] = None,
        attribute_id: Optional[str] = None,
        position: Optional[float] = None,
        is_required: Optional[bool] = None,
        required_channels: Optional[str] = None,
        created_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        One link between a family and an attribute — the row that puts an attribute INTO a family's form. It carries the family's own ordering of that attribute, which overrides the attribute's default position, and `is_required`, which is the flag `POST /products/{id}/completeness` measures and nothing else reads. `required_channels` narrows "required" to named channels; null or empty means required EVERYWHERE, not nowhere.
        
        Every column of `family_attributes` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        family_id : Optional[str]
            Exact match on `family_id`. The family this link belongs to — one side of the pair that makes an attribute part of a family's form.
        attribute_id : Optional[str]
            Exact match on `attribute_id`. The attribute the family carries. One row per (family, attribute); deleting either side deletes the link.
        position : Optional[float]
            Exact match on `position`. The family's own ordering of this attribute, which overrides the attribute's default `position` in this family's form.
        is_required : Optional[bool]
            Exact match on `is_required`. The attribute has to carry a value for a product of this family to count as complete. `POST /products/{id}/completeness` measures exactly these and nothing else.
        required_channels : Optional[str]
            Exact match on `required_channels`. Narrows `is_required` to named channels. NULL or an empty list means required EVERYWHERE, not nowhere — that is how every required link in the wild is stored, and reading an empty list as "nowhere" reports a fully configured family as demanding nothing. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_attributes'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if family_id is not None:
            api_params['family_id'] = self._normalize_value(family_id)
        if attribute_id is not None:
            api_params['attribute_id'] = self._normalize_value(attribute_id)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        if is_required is not None:
            api_params['is_required'] = self._normalize_value(is_required)
        if required_channels is not None:
            api_params['required_channels'] = self._normalize_value(required_channels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_family_attributes_create(
        self,
        attribute_id: str,
        family_id: str,
        is_required: Optional[bool] = None,
        position: Optional[float] = None,
        required_channels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one family attribute and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        One link between a family and an attribute — the row that puts an attribute INTO a family's form. It carries the family's own ordering of that attribute, which overrides the attribute's default position, and `is_required`, which is the flag `POST /products/{id}/completeness` measures and nothing else reads. `required_channels` narrows "required" to named channels; null or empty means required EVERYWHERE, not nowhere.
        
        `family_id` and `attribute_id` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `family_id` and `attribute_id` answers 409.

        Parameters
        ----------
        attribute_id : str
            The attribute the family carries. One row per (family, attribute); deleting either side deletes the link.
        family_id : str
            The family this link belongs to — one side of the pair that makes an attribute part of a family's form.
        is_required : Optional[bool]
            The attribute has to carry a value for a product of this family to count as complete. `POST /products/{id}/completeness` measures exactly these and nothing else.
        position : Optional[float]
            The family's own ordering of this attribute, which overrides the attribute's default `position` in this family's form.
        required_channels : Optional[Dict[str, Any]]
            Narrows `is_required` to named channels. NULL or an empty list means required EVERYWHERE, not nowhere — that is how every required link in the wild is stored, and reading an empty list as "nowhere" reports a fully configured family as demanding nothing.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_attributes'
        api_params = {}
        if attribute_id is None:
            raise RevenexxException('Missing required parameter: "attribute_id"')

        if family_id is None:
            raise RevenexxException('Missing required parameter: "family_id"')


        api_params['attribute_id'] = self._normalize_value(attribute_id)
        api_params['family_id'] = self._normalize_value(family_id)
        if is_required is not None:
            api_params['is_required'] = self._normalize_value(is_required)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        api_params['required_channels'] = self._normalize_value(required_channels)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_attributes_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one family attribute by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        Nothing in this schema references it, so nothing else changes.
        
        An id no family attribute of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `family_attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_attributes`. An id no family attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_attributes_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one family attribute by its id — the whole row, every column, as it is stored.
        
        One link between a family and an attribute — the row that puts an attribute INTO a family's form. It carries the family's own ordering of that attribute, which overrides the attribute's default position, and `is_required`, which is the flag `POST /products/{id}/completeness` measures and nothing else reads. `required_channels` narrows "required" to named channels; null or empty means required EVERYWHERE, not nowhere.
        
        An id no family attribute of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `family_attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_attributes`. An id no family attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_attributes_update(
        self,
        id: str,
        attribute_id: Optional[str] = None,
        family_id: Optional[str] = None,
        is_required: Optional[bool] = None,
        position: Optional[float] = None,
        required_channels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one family attribute by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        One link between a family and an attribute — the row that puts an attribute INTO a family's form. It carries the family's own ordering of that attribute, which overrides the attribute's default position, and `is_required`, which is the flag `POST /products/{id}/completeness` measures and nothing else reads. `required_channels` narrows "required" to named channels; null or empty means required EVERYWHERE, not nowhere.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `family_id` and `attribute_id` answers 409.

        Parameters
        ----------
        id : str
            The `family_attributes` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_attributes`. An id no family attribute of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        attribute_id : Optional[str]
            The attribute the family carries. One row per (family, attribute); deleting either side deletes the link.
        family_id : Optional[str]
            The family this link belongs to — one side of the pair that makes an attribute part of a family's form.
        is_required : Optional[bool]
            The attribute has to carry a value for a product of this family to count as complete. `POST /products/{id}/completeness` measures exactly these and nothing else.
        position : Optional[float]
            The family's own ordering of this attribute, which overrides the attribute's default `position` in this family's form.
        required_channels : Optional[Dict[str, Any]]
            Narrows `is_required` to named channels. NULL or an empty list means required EVERYWHERE, not nowhere — that is how every required link in the wild is stored, and reading an empty list as "nowhere" reports a fully configured family as demanding nothing.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_attributes/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if attribute_id is not None:
            api_params['attribute_id'] = self._normalize_value(attribute_id)
        if family_id is not None:
            api_params['family_id'] = self._normalize_value(family_id)
        if is_required is not None:
            api_params['is_required'] = self._normalize_value(is_required)
        if position is not None:
            api_params['position'] = self._normalize_value(position)
        api_params['required_channels'] = self._normalize_value(required_channels)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_variants_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        family_id: Optional[str] = None,
        code: Optional[str] = None,
        labels: Optional[str] = None,
        axes: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        A variant structure of a family: the attribute axes a product model splits its variants on — colour, then size. A product follows one through `family_variant_id`, and an attribute named as an axis becomes read-only on the model and is set on each variant instead, which is what `GET /products/attribute-schema` reports as `readonly_reason`. Two axis shapes are in the wild and both are read: a bare list of codes, or one entry per level.
        
        Every column of `family_variants` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        family_id : Optional[str]
            Exact match on `family_id`. The family this variant structure belongs to. A family may carry several, and a product names the one it follows through `family_variant_id`.
        code : Optional[str]
            Exact match on `code`. The variant structure's stable identifier — how this family splits, not which product it splits. Unique per tenant.
        labels : Optional[str]
            Exact match on `labels`. What the variant structure is called, per language tag. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        axes : Optional[str]
            Exact match on `axes`. The attribute codes a product model splits its variants on. Two shapes are in the wild and both are read: a bare list of codes, or one entry per level, outermost first — `[{"level": 1, "axes": ["colour"]}, {"level": 2, "axes": ["size"]}]`. An attribute named here is READ-ONLY on the model and set on each variant, which is what `AttributeField.readonly_reason` reports. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_variants'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if family_id is not None:
            api_params['family_id'] = self._normalize_value(family_id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if axes is not None:
            api_params['axes'] = self._normalize_value(axes)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_family_variants_create(
        self,
        code: str,
        family_id: str,
        axes: Optional[Dict[str, Any]] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one family variant and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        A variant structure of a family: the attribute axes a product model splits its variants on — colour, then size. A product follows one through `family_variant_id`, and an attribute named as an axis becomes read-only on the model and is set on each variant instead, which is what `GET /products/attribute-schema` reports as `readonly_reason`. Two axis shapes are in the wild and both are read: a bare list of codes, or one entry per level.
        
        `family_id` and `code` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The variant structure's stable identifier — how this family splits, not which product it splits. Unique per tenant.
        family_id : str
            The family this variant structure belongs to. A family may carry several, and a product names the one it follows through `family_variant_id`.
        axes : Optional[Dict[str, Any]]
            The attribute codes a product model splits its variants on. Two shapes are in the wild and both are read: a bare list of codes, or one entry per level, outermost first — `[{"level": 1, "axes": ["colour"]}, {"level": 2, "axes": ["size"]}]`. An attribute named here is READ-ONLY on the model and set on each variant, which is what `AttributeField.readonly_reason` reports.
        labels : Optional[Dict[str, Any]]
            What the variant structure is called, per language tag.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_variants'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')

        if family_id is None:
            raise RevenexxException('Missing required parameter: "family_id"')


        api_params['axes'] = self._normalize_value(axes)
        api_params['code'] = self._normalize_value(code)
        api_params['family_id'] = self._normalize_value(family_id)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_variants_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one family variant by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        `products.family_variant_id` is set to null instead, so the rows that pointed at it survive the delete rather than going with it.
        
        An id no family variant of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `family_variants` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_variants`. An id no family variant of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_variants/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_variants_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one family variant by its id — the whole row, every column, as it is stored.
        
        A variant structure of a family: the attribute axes a product model splits its variants on — colour, then size. A product follows one through `family_variant_id`, and an attribute named as an axis becomes read-only on the model and is set on each variant instead, which is what `GET /products/attribute-schema` reports as `readonly_reason`. Two axis shapes are in the wild and both are read: a bare list of codes, or one entry per level.
        
        An id no family variant of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `family_variants` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_variants`. An id no family variant of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_variants/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_family_variants_update(
        self,
        id: str,
        axes: Optional[Dict[str, Any]] = None,
        code: Optional[str] = None,
        family_id: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one family variant by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        A variant structure of a family: the attribute axes a product model splits its variants on — colour, then size. A product follows one through `family_variant_id`, and an attribute named as an axis becomes read-only on the model and is set on each variant instead, which is what `GET /products/attribute-schema` reports as `readonly_reason`. Two axis shapes are in the wild and both are read: a bare list of codes, or one entry per level.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `family_variants` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/family_variants`. An id no family variant of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        axes : Optional[Dict[str, Any]]
            The attribute codes a product model splits its variants on. Two shapes are in the wild and both are read: a bare list of codes, or one entry per level, outermost first — `[{"level": 1, "axes": ["colour"]}, {"level": 2, "axes": ["size"]}]`. An attribute named here is READ-ONLY on the model and set on each variant, which is what `AttributeField.readonly_reason` reports.
        code : Optional[str]
            The variant structure's stable identifier — how this family splits, not which product it splits. Unique per tenant.
        family_id : Optional[str]
            The family this variant structure belongs to. A family may carry several, and a product names the one it follows through `family_variant_id`.
        labels : Optional[Dict[str, Any]]
            What the variant structure is called, per language tag.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/family_variants/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        api_params['axes'] = self._normalize_value(axes)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if family_id is not None:
            api_params['family_id'] = self._normalize_value(family_id)
        api_params['labels'] = self._normalize_value(labels)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_measurement_families_list(
        self,
        limit: Optional[float] = None,
        offset: Optional[float] = None,
        order: Optional[str] = None,
        id: Optional[str] = None,
        code: Optional[str] = None,
        standard_unit: Optional[str] = None,
        units: Optional[str] = None,
        labels: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None
    ) -> Dict[str, Any]:
        """
        A family of units and the standard one they all convert to — weight in kilograms, length in metres. A `measure` attribute names one and then offers exactly that family's units, and each unit's `convert_factor` is what makes two values recorded in different units comparable at all.
        
        Every column of `measurement_families` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        limit : Optional[float]
            Page size (default 50, max 200). A larger value is clamped rather than refused.
        offset : Optional[float]
            Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
        order : Optional[str]
            Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
        id : Optional[str]
            Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
        code : Optional[str]
            Exact match on `code`. The measurement family's stable identifier. A `measure` attribute names one and then offers that family's units.
        standard_unit : Optional[str]
            Exact match on `standard_unit`. The unit every value of this family is converted to before it is compared or sorted — the unit each `convert_factor` is relative to.
        units : Optional[str]
            Exact match on `units`. The units this family offers. `convert_factor` multiplies a value into `standard_unit`, so a gram is 0.001 kilograms; `symbol` is what a form prints next to the number. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        labels : Optional[str]
            Exact match on `labels`. What the measurement family is called, per language tag. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
        created_at : Optional[str]
            Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
        updated_at : Optional[str]
            Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
        
        Returns
        -------
        Dict[str, Any]
            API response as a dictionary
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/measurement_families'
        api_params = {}

        if limit is not None:
            api_params['limit'] = self._normalize_value(limit)
        if offset is not None:
            api_params['offset'] = self._normalize_value(offset)
        if order is not None:
            api_params['order'] = self._normalize_value(order)
        if id is not None:
            api_params['id'] = self._normalize_value(id)
        if code is not None:
            api_params['code'] = self._normalize_value(code)
        if standard_unit is not None:
            api_params['standard_unit'] = self._normalize_value(standard_unit)
        if units is not None:
            api_params['units'] = self._normalize_value(units)
        if labels is not None:
            api_params['labels'] = self._normalize_value(labels)
        if created_at is not None:
            api_params['created_at'] = self._normalize_value(created_at)
        if updated_at is not None:
            api_params['updated_at'] = self._normalize_value(updated_at)

        response = self.client.call('get', api_path, {
        }, api_params)

        return response


    def products_measurement_families_create(
        self,
        code: str,
        standard_unit: str,
        labels: Optional[Dict[str, Any]] = None,
        units: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Creates one measurement family and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
        
        A family of units and the standard one they all convert to — weight in kilograms, length in metres. A `measure` attribute names one and then offers exactly that family's units, and each unit's `convert_factor` is what makes two values recorded in different units comparable at all.
        
        `code` and `standard_unit` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.

        Parameters
        ----------
        code : str
            The measurement family's stable identifier. A `measure` attribute names one and then offers that family's units.
        standard_unit : str
            The unit every value of this family is converted to before it is compared or sorted — the unit each `convert_factor` is relative to.
        labels : Optional[Dict[str, Any]]
            What the measurement family is called, per language tag.
        units : Optional[Dict[str, Any]]
            The units this family offers. `convert_factor` multiplies a value into `standard_unit`, so a gram is 0.001 kilograms; `symbol` is what a form prints next to the number.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/measurement_families'
        api_params = {}
        if code is None:
            raise RevenexxException('Missing required parameter: "code"')

        if standard_unit is None:
            raise RevenexxException('Missing required parameter: "standard_unit"')


        api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        api_params['standard_unit'] = self._normalize_value(standard_unit)
        api_params['units'] = self._normalize_value(units)

        response = self.client.call('post', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_measurement_families_delete(
        self,
        id: str
    ) -> Error:
        """
        Deletes one measurement family by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
        
        Nothing in this schema references it, so nothing else changes.
        
        An id no measurement family of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.

        Parameters
        ----------
        id : str
            The `measurement_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/measurement_families`. An id no measurement familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/measurement_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('delete', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_measurement_families_get(
        self,
        id: str
    ) -> Error:
        """
        Reads one measurement family by its id — the whole row, every column, as it is stored.
        
        A family of units and the standard one they all convert to — weight in kilograms, length in metres. A `measure` attribute names one and then offers exactly that family's units, and each unit's `convert_factor` is what makes two values recorded in different units comparable at all.
        
        An id no measurement family of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
        
        Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.

        Parameters
        ----------
        id : str
            The `measurement_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/measurement_families`. An id no measurement familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/measurement_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))


        response = self.client.call('get', api_path, {
        }, api_params)

        return self._parse_response(response, model=Error)


    def products_measurement_families_update(
        self,
        id: str,
        code: Optional[str] = None,
        labels: Optional[Dict[str, Any]] = None,
        standard_unit: Optional[str] = None,
        units: Optional[Dict[str, Any]] = None
    ) -> Error:
        """
        Updates one measurement family by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
        
        A family of units and the standard one they all convert to — weight in kilograms, length in metres. A `measure` attribute names one and then offers exactly that family's units, and each unit's `convert_factor` is what makes two values recorded in different units comparable at all.
        
        A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.

        Parameters
        ----------
        id : str
            The `measurement_families` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/measurement_families`. An id no measurement familie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
        code : Optional[str]
            The measurement family's stable identifier. A `measure` attribute names one and then offers that family's units.
        labels : Optional[Dict[str, Any]]
            What the measurement family is called, per language tag.
        standard_unit : Optional[str]
            The unit every value of this family is converted to before it is compared or sorted — the unit each `convert_factor` is relative to.
        units : Optional[Dict[str, Any]]
            The units this family offers. `convert_factor` multiplies a value into `standard_unit`, so a gram is 0.001 kilograms; `symbol` is what a form prints next to the number.
        
        Returns
        -------
        Error
            API response as a typed Pydantic model
        
        Raises
        ------
        RevenexxException
            If API request fails
        """

        api_path = '/v1/products/measurement_families/{id}'
        api_params = {}
        if id is None:
            raise RevenexxException('Missing required parameter: "id"')

        api_path = api_path.replace('{id}', str(self._normalize_value(id)))

        if code is not None:
            api_params['code'] = self._normalize_value(code)
        api_params['labels'] = self._normalize_value(labels)
        if standard_unit is not None:
            api_params['standard_unit'] = self._normalize_value(standard_unit)
        api_params['units'] = self._normalize_value(units)

        response = self.client.call('put', api_path, {
            'content-type': 'application/json',
        }, api_params)

        return self._parse_response(response, model=Error)

